name: core_infrastructure
description: "Core infrastructure - event bus, dependency injection, navigation service."
version: 0.0.1
environment:
  sdk: ^3.10.3
resolution: workspace

dependencies:
  shared:
  get_it: ^8.0.3
  injectable: ^2.5.0
  go_router: ^17.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.25.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.7.0
  build_runner: ^2.4.15
