name: locations
description: "Location management feature - bounded context for apiary locations."
version: 0.0.1
environment:
  sdk: ^3.10.3
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared:

dev_dependencies:
  flutter_test:
    sdk: flutter
