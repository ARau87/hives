name: ui
description: "Ui library for the Hives project."
version: 0.0.1
environment:
  sdk: ^3.10.3
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  # Pinned to ^6.x for stability with Flutter SDK ^3.10.3.
  # Latest is 8.x — verify compatibility before upgrading.
  google_fonts: ^6.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
